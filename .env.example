# Comma-separated list of scraper keys to run, matching the keys registered
# in scrapers/__init__.py (SCRAPERS). Whitespace is trimmed and case doesn't
# matter. If unset, every registered scraper runs.
ENABLED_COMPANIES=accenture,bain,two_sigma
